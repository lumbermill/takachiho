<div class="row">
<div class="col-sm-6">
<%= form_for(@setting) do |f| %>
  <% if @setting.errors.any? %>
    <div class="alert alert-warning">
      <h2><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h2>
      <ul>
      <% @setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :raspi_id, "ID" %>
    <input type="text" value="<%= @setting.id.present? ? @setting.id : "*(自動割当)" %>" class="form-control" disabled="disabled"/>
    <%= f.hidden_field :raspi_id %>
  </div>
  <div class="form-group">
    <%= f.label :name, t("activerecord.attributes.setting.name") %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :min_tmpr, t("activerecord.attributes.setting.min_tmpr") %>
    <%= f.text_field :min_tmpr, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :max_tmpr, t("activerecord.attributes.setting.max_tmpr") %>
    <%= f.text_field :max_tmpr, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.fields_for :sakura_iot_modules do |sim| %>
      <%= sim.label :token, t("activerecord.attributes.setting.token4sakura") %>
      <%= sim.text_field :token, class: "form-control" %>
    <% end %>
  </div>
  <div class="form-group">
    <%= f.label :city_id, t("activerecord.attributes.setting.city_id") %>
    <%= f.select :city_id, options_for_select(city_list.collect { |u| [u["name"], u["id"]] }) %>
  </div>
  <%= f.submit "保存する", class: "btn btn-primary" %>
<% end %>
</div>
</div>
<hr/>
